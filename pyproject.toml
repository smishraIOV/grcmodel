[project]
name = "claude-code-101"
version = "0.1.0"
description = "GRC framework and quantitative model"
readme = "readme.md"
authors = [
    { name = "smishraiov", email = "shreemoy@rootstocklabs.com" }
]
requires-python = ">=3.13"
dependencies = [
    "torch>=2.14.0",
]

[tool.uv]
package = false

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
